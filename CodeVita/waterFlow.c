#include <stdio.h>
#define SIZE 10000

int main()
{
	int N, i, max;
	int buket[SIZE], B[SIZE], C[SIZE];
	scanf("%d", &N);
	
	for(i=0; i<N; i++)
		scanf("%d %d %d", &A[i], &B[i], &C[i]);
		
	for(i=0; i<N; i++)
	{
		max = (C[i]> ((B[i]<A[i]?B[i]:A[i])));
		
		while(1)
		{
			if(max==T[i])
				C[i]--;
			if(max ==B[i] &&
			
		printf("0");
	}
	return 0;
}

